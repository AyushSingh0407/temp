const sayHi = (name) => {
    console.log(`hello there ${name}`);
}



const score = (name, run) => {
    console.log(`${name} have scored ${run}`);
}

score("ayush", "78");

function add(n1, n2) {
    console.log(`${n1 + n2}`)
}

add(3, 9);